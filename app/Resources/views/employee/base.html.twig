{% extends '::layout.html.twig' %}
{% import '::macros.html.twig' as macro %}

{% block title %}{{ 'employeesManagement' | trans }}{% endblock %}

{% block breadcrumb %}
    {{ macro.breadcrumbItem(organisation.name, 'app_organisation_info', {'organisation_id': organisation.id, 'organisation_slug': organisation.slug}) }}
    {{ macro.breadcrumbItem('employees'|trans, 'app_employee_list', {'organisation_id': organisation.id, 'organisation_slug': organisation.slug}) }}
{% endblock %}